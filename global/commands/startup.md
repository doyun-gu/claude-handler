Re-run the session startup protocol from the global CLAUDE.md. Follow the 3-branch decision tree:

1. **Check for project CLAUDE.md** — if it exists, read it and confirm you're ready to work.
2. **Check for existing code** — if code exists but no CLAUDE.md, auto-scan the project (tech stack, git history, directory structure, README, build scripts, file count). Present what you found. Ask only what you can't detect: product idea, target user, problem solved, commitment level, current priorities. Offer to generate a CLAUDE.md.
3. **Empty project** — run full onboarding questions: product idea, target user, problem solved, tech preferences, commitment level, first task.

Additionally, if the project already has a CLAUDE.md:
- Check if it seems outdated (e.g., mentions files that no longer exist, missing new major directories).
- If discrepancies are found, flag them and offer to update.
